"use client";
import {FormEvent,useEffect,useState}from"react";import{IoAdd,IoClose}from"react-icons/io5";import HabitantePage,{EmptyState,ModuleHeader}from"@/components/HabitantePage";import Pagination from"@/components/Pagination";import{useAuth}from"@/features/authentication/AuthContext";import{getAreas}from"@/services/areas";import{crearReservacion,getReservaciones}from"@/services/reservaciones";import type{Area}from"@/types/areas";import type{Reservacion}from"@/types/reservaciones";
export default function ReservacionesHabitante(){const{token,activeMembership}=useAuth();const[items,setItems]=useState<Reservacion[]>([]);const[areas,setAreas]=useState<Area[]>([]);const[search,setSearch]=useState('');const[page,setPage]=useState(1);const[count,setCount]=useState(0);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[open,setOpen]=useState(false);const[form,setForm]=useState({area:'',fecha:'',hora_inicio:'',hora_fin:'',num_asistentes:1,descripcion:''});
const load=async()=>{if(!token||!activeMembership)return;setLoading(true);try{const[d,a]=await Promise.all([getReservaciones(token,{privada:activeMembership.privada,search:search||undefined,page}),getAreas(token,{privada:activeMembership.privada})]);setItems(d.results);setCount(d.count);setAreas(a.results);setError('')}catch(e){setError(e instanceof Error?e.message:'No se pudieron cargar las reservaciones.')}finally{setLoading(false)}};
// eslint-disable-next-line react-hooks/set-state-in-effect, react-hooks/exhaustive-deps
useEffect(()=>{void load()},[token,activeMembership,search,page]);const submit=async(e:FormEvent)=>{e.preventDefault();if(!token)return;try{await crearReservacion(token,form);setOpen(false);setForm({area:'',fecha:'',hora_inicio:'',hora_fin:'',num_asistentes:1,descripcion:''});await load()}catch(err){setError(err instanceof Error?err.message:'No se pudo reservar.')}};
return <HabitantePage activeItem="Reservaciones"><ModuleHeader title="Mis reservaciones" subtitle="Reserva y consulta las áreas comunes de tu privada." search={search} onSearch={v=>{setSearch(v);setPage(1)}} action={<button onClick={()=>setOpen(true)} className="flex items-center gap-2 rounded-xl bg-[#0a496a] px-6 py-4 font-bold text-white"><IoAdd/>Nueva reservación</button>}/><EmptyState loading={loading} error={error} empty={!items.length}/>{!loading&&!error&&items.length>0&&<div className="overflow-hidden rounded-2xl bg-white shadow"><table className="w-full"><thead className="bg-[#0a496a] text-white"><tr>{['Área','Fecha','Horario','Asistentes','Estado'].map(h=><th key={h} className="p-4 text-left">{h}</th>)}</tr></thead><tbody>{items.map(i=><tr key={i.id} className="border-b even:bg-slate-50"><td className="p-4 font-semibold">{i.area_nombre}</td><td>{new Date(`${i.fecha}T12:00`).toLocaleDateString('es-MX')}</td><td>{i.hora_inicio.slice(0,5)} - {i.hora_fin.slice(0,5)}</td><td>{i.num_asistentes}</td><td><span className="rounded-lg bg-orange-100 px-3 py-2 capitalize">{i.estado}</span></td></tr>)}</tbody></table></div>}<Pagination currentPage={page} totalPages={Math.max(1,Math.ceil(count/20))} onPageChange={setPage}/>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><form onSubmit={submit} className="w-full max-w-xl rounded-3xl bg-white p-7"><div className="flex justify-between"><h2 className="text-2xl font-bold text-[#0a496a]">Nueva reservación</h2><button type="button" onClick={()=>setOpen(false)}><IoClose size={28}/></button></div><div className="mt-5 grid gap-4"><select required value={form.area} onChange={e=>setForm({...form,area:e.target.value})} className="rounded-xl border p-3"><option value="">Selecciona un área</option>{areas.map(a=><option key={a.id} value={a.id}>{a.nombre} (capacidad {a.capacidad})</option>)}</select><input required type="date" value={form.fecha} onChange={e=>setForm({...form,fecha:e.target.value})} className="rounded-xl border p-3"/><div className="grid grid-cols-2 gap-4"><input required type="time" value={form.hora_inicio} onChange={e=>setForm({...form,hora_inicio:e.target.value})} className="rounded-xl border p-3"/><input required type="time" value={form.hora_fin} onChange={e=>setForm({...form,hora_fin:e.target.value})} className="rounded-xl border p-3"/></div><input required min={1} type="number" value={form.num_asistentes} onChange={e=>setForm({...form,num_asistentes:Number(e.target.value)})} className="rounded-xl border p-3" placeholder="Número de asistentes"/><textarea value={form.descripcion} onChange={e=>setForm({...form,descripcion:e.target.value})} className="rounded-xl border p-3" placeholder="Descripción o notas"/><button className="rounded-xl bg-[#0a496a] p-4 font-bold text-white">Solicitar reservación</button></div></form></div>}</HabitantePage>}
